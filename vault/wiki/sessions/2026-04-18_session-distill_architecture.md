# Session distill architecture (redacted)

This document was removed from git history because it contained hardcoded example secrets.

The canonical guidance is:

- Never store tokens/keys in markdown notes committed to git
- Use env vars (and keep `.env` untracked)
- Prefer sanitized examples with `REDACTED` placeholders

