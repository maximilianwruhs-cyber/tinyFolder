# Mode: art — matches shipped art profile ethos (adjust paths/model as needed)
GZMO_PROFILE=art

# Think stays lightweight unless you deliberately opt in:
GZMO_THINK_RETRIEVAL=off

# Optional excerpt bridge from recent cabinet prose (mostly harmless when empty)
GZMO_MEMORY_WORKING_SET=off
